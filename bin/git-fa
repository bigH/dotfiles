#!/usr/bin/env bash

git_fa() {
  local files targets
  files=$(git ls-files -m -o --exclude-standard) || return
  targets=$(echo "$files" | fzf -m) || return
  echo git add $(echo "$targets") &&
  git add $(echo "$targets")
}

git_fa
