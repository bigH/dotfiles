#!/usr/bin/env bash

# TODO - preview
git_fa() {
  local files targets
  files=$(git ls-files -m -o --exclude-standard) || return
  targets=$(echo "$files" | fzf -m --preview="git diff {}") || return
  echo git add $(echo "$targets") &&
  git add $(echo "$targets")
}

git_fa
