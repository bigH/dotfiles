# retag - build tag file again
retag() {
  echo "retagging pay-server..."
  cd ~/stripe/pay-server
  ripper-tags -f .tags -R --exclude=vendor --exclude=node_modules --exclude=puppet-config --exclude=build/autoloader --exclude=ruby-typer
  echo "done."
}
