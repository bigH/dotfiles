#!/usr/bin/env bash

# Add `journal` path
if [ -d "$HOME/journal" ]; then
  export JOURNAL_PATH="$HOME/journal"

  if [ -e "$JOURNAL_PATH/.journal.init" ]; then
    source "$JOURNAL_PATH/.journal.init"
  fi
fi

